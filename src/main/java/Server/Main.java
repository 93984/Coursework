package Server;

public class Main {
    /*
    function score(total)
        maxScore = 750;
        maxSeconds = 3*60;
        x = 0;
        while TRUE:
            x += 0.02
            time.sleep(0.02)
            if submit button onclick then
                if x > 180 then
                    return total + 1
                else
                    points = int(maxScore*(maxScore^(-x/maxSeconds)))
                    return total + points

    total = 0;
    System.out.println(score(total));
    total += score(total);
     */
}
